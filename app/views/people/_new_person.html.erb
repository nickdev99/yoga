<div class="provider">
  <h2><%= @provider.name %></h2>
</div>
<div class="new_person_title">
  <h2 id="schedule_name">Add New Person</h2>
</div>
<div class="new_person_content">
    <p>Note: Start typing in their first or last name, or email and auto-suggest will let you see if they are already on ILY. Select their name if you see them listed!</p>
    <div class="auto-suggest">
      <div><span>First Name</span> <%= f.text_field :first_name, :id => :first_name-%></div>
      <div><span>Last Name</span> <%= f.text_field :last_name, :id => :last_name -%></div>
      <div><span>Email</span> <%= f.text_field :email, :id => :email -%></div>      
    </div>
    <p>Note: Email is required for anyone not already on ILY. We will send them an invitation on your behalf if they aren't on ILY yet. If you don't know their email, please leave Instructor blank for now.</p>        
    <div class="clear"></div>
    <div><h3>Roles</h3></div>
    <div><%= check_box_tag 'roles[instructor]', nil, @roles ? @roles.include?('instructor') : false-%><span>Instructor</span></div>
    <div><%= check_box_tag 'roles[staff]', nil, @roles ? @roles.include?('staff') : false -%><span>Staff</span></div>
    <div><%= check_box_tag 'roles[guest]', nil, @roles ? @roles.include?('guest') : false -%><span>Guest</span></div>
    <div><%= check_box_tag 'roles[volunteer]', nil, @roles ? @roles.include?('volunteer') : false -%><span>Energy Exchanger/Volunteer</span></div>
    <h3>Admin Permissions</h3>
    <div><%= check_box_tag :all_studios, nil, @studios ? @provider.studios.length == @studios.length : false-%><span>All Studios</span></div>
    <% @provider.studios.each do |studio| %>
      <div><%= check_box_tag "[studios]studio_#{studio.id}", nil, @studios ? @studios.include?(studio.id) :false -%><span><%= studio.name %></span></div>
    <% end %>
    <%= f.submit "Add to #{@provider.name}" %>
</div>